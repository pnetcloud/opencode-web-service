# npm Automation token (for local publishing, bypassing CI)
# Create at: npmjs.com → Access Tokens → Generate → Automation
# With Trusted Publishers in CI, this file is only needed for local publishing
NPM_TOKEN=
